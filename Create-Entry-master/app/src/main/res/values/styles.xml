<?xml version="1.0" encoding="utf-8"?>

    <resources xmlns:tools="http://schemas.android.com/tools">

        <!-- Base application theme. -->
        <style name="Theme.Base.MyTheme" parent="AppTheme">
            <!-- Customize your theme here. -->
            <item name="android:colorPrimary">@color/colorPrimary</item>
            <item name="android:colorPrimaryDark">@color/colorPrimaryDark</item>
            <item name="android:colorAccent">@color/colorAccent</item>
        </style>

        <!-- Style for custom white radio button color -->
        <style name="WhiteRadioButton" parent="Theme.Base.MyTheme">
            <item name="android:colorControlNormal">@android:color/white</item>
            <item name="android:colorControlActivated">@android:color/white</item>
        </style>

        <style name="TextAppearance.Appcompact.Medium" parent="android:TextAppearance.DeviceDefault.Medium">

        </style>
    <style name="TextAppearance.Appcompact.Large" parent="android:TextAppearance.DeviceDefault.Large"></style>
    <style name="TextAppearance.Appcompact.small" parent="android:TextAppearance.DeviceDefault.Small"></style>

    </resources>
